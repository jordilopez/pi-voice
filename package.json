{
  "name": "pi-voice",
  "version": "0.1.0",
  "description": "Voice commands (push-to-talk STT) and spoken feedback (TTS) for pi-coding-agent",
  "keywords": ["pi-package"],
  "type": "module",
  "private": true,
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "check": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7"
  }
}
